{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities the lattice window is granted.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
